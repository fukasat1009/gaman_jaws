
   <%= attachment_image_tag @user, :profile_image, format: 'jpeg' , size: "200x150" %>
    <%= @user.name %>
    <%= @user.body %>
    <% if @user == current_user %>
      <%= link_to "編集画面",edit_user_path(@user) %>
    <% end %>
 