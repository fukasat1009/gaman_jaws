<div class="window">
  <div class="box header-show">
  <div class="img-show">
   <%= attachment_image_tag @user, :profile_image, format: 'jpeg' , size: "200x150" %>
  </div>
    <h2><%= @user.name %></h2>
    <h4><%= @user.body %></h4>
    <% if @user == current_user %>
      <h4><%= link_to "編集画面",edit_user_path(@user) %></h4>
    <% end %>
  </div>
</div>